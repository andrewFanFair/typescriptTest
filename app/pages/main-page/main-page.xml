<Page xmlns="http://schemas.nativescript.org/tns.xsd" loaded="pageLoaded">
    <GridLayout rows="auto, *">
      <!--<header>-->
      <StackLayout cssClass="page-header">
          <Label text="Header" cssClass="page-title bold" horizontalAlignment="center" margin="15"/>
      </StackLayout>

      <!--Session Views-->
      <GridLayout rows="auto, *" row="1">
          <ListView items="{{ sessions }}" row="1">
              <ListView.itemTemplate>
                  <GridLayout colums="auto, *">
                        <GridLayout columns="auto" rows="auto" cssClass="favourite-wrap">
                                <Image id="imgFav" stretch="none" cssClass="icon-image" src="res://ic_add_to_fav" />
                        </GridLayout>

                        <StackLayout col="1" cssClass="fix-layout">
                                <Label text="{{ title }}" textWrap="true" />
                                <Label text="{{ room }}" cssClass="info-orange" />
                        </StackLayout>
                  </GridLayout>
              </ListView.itemTemplate>
          </ListView>
      </GridLayout>
    </GridLayout>
</Page>